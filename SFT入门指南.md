# SFT 入门指南：让大模型学会听话

## 一、什么是大模型

大模型就是**一大堆数字**（叫"参数"），比如 Qwen2.5-0.5B 有 5 亿个参数。

这些数字组成一个流水线：

```
你输入一句话 → 模型一个字一个字地预测下一个字 → 输出回答
```

模型内部长这样：

```
"请你给哪吒写一首诗"
      ↓
  Embedding 层（1.36亿参数）     ← 查字典，把每个字变成一串数字
      ↓
  Transformer 第 1 层           ← 浅层：认字、识别词性
  Transformer 第 2 层
  ...
  Transformer 第 12 层          ← 中层：理解句子含义
  ...
  Transformer 第 24 层          ← 深层：综合理解，构思答案
      ↓
  输出层                        ← 预测下一个字
```

- **Embedding** = 认字（查表，把文字变成数字向量，每个字互不影响）
- **Transformer** = 读懂句子（让字和字之间互相看，理解上下文）
- 24 层结构一样（注意力 + 全连接），但各自学到不同深度的理解

这类模型只用 **Decoder**（生成型），每个字只能看到前面的字，不能偷看后面的，因为后面的字还没生出来。

## 二、什么是 SFT

SFT = Supervised Fine-Tuning = 有监督微调。

大模型的训练分两个阶段：

```
预训练：读了几万亿字的书（维基百科、网页、代码...）
        → 学会了语言能力，但不听指令，像个读了很多书但没上过班的人

SFT：   用标注好的"问题→回答"数据再训练一下
        → 学会了按指令回答问题，像入职第一天老板教你怎么干活
```

**一句话：预训练给了模型语言能力，SFT 教它听话。**

## 三、SFT 的核心原理

SFT 和预训练的代码流程基本一致，唯一的区别：**不对用户的指令部分计算 loss，只对回答部分计算 loss**。

### loss 是什么

模型预测下一个字时会给出概率：

```
正确答案是 "恩"

训练前: "恩" 的概率 5%   → loss 大（猜错了）
训练后: "恩" 的概率 95%  → loss 小（猜对了）
```

**loss 大 = 猜错了，loss 小 = 猜对了。训练就是不断调参数让 loss 变小。**

### 怎么做到"只对回答算 loss"

把指令部分的 label 设为 -100，PyTorch 会自动跳过：

```
输入序列:  请你给敖丙写一首诗：碧海生龙子，云中舞雪霜。恩仇难两忘，何处是家乡？<eos>
          |_________ 指令+上文 ___________||________ 回答 ________|
labels:   -100  -100  -100 ... -100  -100   恩仇难两忘...    <eos>
          |______ 不算loss，跳过 ______|   |____ 只算这部分 ____|
```

类比：老师出题 "1+1=?"，SFT 只让学生记住答案 "2"，不用学背题目。

## 四、训练过程

### 数据

本 demo 用了 5 条哪吒角色诗歌数据：

| instruct（指令） | input（上文） | label（期望输出） |
|------|------|------|
| 请你给哪吒写一首诗： | 哪吒降世，意气飞扬... | 红绫缠腕，风火踏浪... |
| 请你给敖丙写一首诗： | 碧海生龙子，云中舞雪霜。 | 恩仇难两忘，何处是家乡？ |
| ...共 5 条 | | |

### Epoch（轮次）

把所有数据从头到尾过一遍 = 1 个 Epoch，就像刷题：

```
Epoch 1:  loss = 4.799    第一遍，完全不会
Epoch 3:  loss = 0.142    第三遍，有点印象了
Epoch 10: loss = 0.0003   第十遍，滚瓜烂熟
```

本 demo 共 5 条数据，batch size 为 2，每个 epoch 3 步，10 个 epoch 共 30 步，38 秒训完。

### 模型发生了什么变化

训练的本质是微调 5 亿个参数的数值，每个参数平均只变了 0.000066：

```
训练前: 参数 = [0.0234, -0.1567, 0.8912, ...]
训练后: 参数 = [0.0234, -0.1566, 0.8913, ...]
                         ↑ 变了一点点  ↑ 变了一点点
```

5 亿个参数各变一点点，累积起来就让模型学会了"怎么回答这 5 首诗"。这种规律无法用人类语言直接表示，它分散编码在整个网络里。

## 五、推理验证

### 测试 1：给指令+上文（训练时见过的完整输入）

```
输入: 请你给敖丙写一首诗：碧海生龙子，云中舞雪霜。
预测: 恩仇难两忘，何处是家乡？   ✅ 和标签完全一致
```

5 条全部精确命中。

### 测试 2：只给指令（训练时没见过的输入方式）

```
输入: 请你给敖丙写一首诗：
预测: （空）                     ❌ 什么都没生成
```

部分能答对，部分答不上来。

### 为什么会这样

模型训练时见到的永远是 `指令+上文 → 回答`，从没见过 `指令 → 回答`。就像服务员只培训了 3 道菜的话术，顾客问第 4 道菜就懵了。

这不是 bug，恰恰证明了 **模型只学到了你让它学的东西**。

## 六、如何增加泛化能力

本 demo 只有 5 条数据，模型只学到了死记硬背——给完整输入能答对，换个说法就不会了。要让模型真正学会"写诗"而不是"背诗"，有以下几个方向：

### 1. 加数据量（最有效）

```
5 条    → 死记硬背（现在的状态）
500 条  → 开始学到一些规律（比如诗的格式、押韵）
5000 条 → 能写没见过的角色的诗了
50000条 → 风格、意境都能把握
```

不光要量大，还要多样：不同角色、不同风格、不同长度，让模型见过各种情况。

### 2. 数据增强（低成本提升）

把现有数据做变换，人工扩充：

```
原始:  "请你给哪吒写一首诗：" + 上文 → 回答
变体1: "为哪吒赋诗一首："    + 上文 → 回答     ← 换指令说法
变体2: "请你给哪吒写一首诗：" + 不同上文 → 不同回答  ← 换内容
变体3: "用五言绝句描写哪吒："  + 上文 → 回答     ← 换格式要求
```

模型见过多种说法，就不会只认一种固定输入了。

### 3. 用更大的模型

```
0.5B（5亿参数）  → 容量小，能记住的规律有限
7B（70亿参数）   → 容量大，同样的数据能学到更深的规律
72B（720亿参数） → 预训练时就已经会写诗了，微调几条就能泛化
```

模型越大，预训练阶段学到的语言能力越强，SFT 需要的数据就越少。

### 4. 调训练策略（防止过拟合）

```
学习率调小       → 不要把预训练学到的能力覆盖掉
早停(Early Stop) → loss 够小就停，别过拟合（背太死了）
加正则化         → dropout、weight decay，防止模型只记住训练数据
减少 epoch       → 10 轮 loss 已经到 0.0003，过度训练反而有害
```

### 5. LoRA：只调一小部分参数（推荐）

```
全量微调: 调全部 5 亿参数 → 容易把预训练学到的能力改坏
LoRA:    冻住原模型，只加几百万个新参数去调 → 保留原有能力，只学新任务
```

LoRA 是目前最主流的微调方式，效果好又省资源。

### 方法对比

| 方法 | 效果 | 成本 |
|------|------|------|
| 加数据量 | 最有效 | 标注数据贵 |
| 数据增强 | 低成本提升 | 需要设计变换规则 |
| 用更大模型 | 天花板更高 | GPU 成本高 |
| 调训练策略 | 防止过拟合 | 需要调参经验 |
| LoRA | 保留预训练能力 | 主流方案，推荐 |

**实际生产中通常是：大模型 + LoRA + 几万条高质量数据，三者结合效果最好。**

## 七、SFT 相关重要概念

### Chat Template（对话模板）

本 demo 用最简单的拼接方式。实际 SFT 都用标准对话模板，用特殊 token 区分角色：

```
<|im_start|>system
你是一个诗人<|im_end|>
<|im_start|>user
请你给哪吒写一首诗<|im_end|>
<|im_start|>assistant
红绫缠腕，风火踏浪。<|im_end|>
```

模型通过这些特殊 token 知道"谁在说话"，才能正确地扮演 assistant 角色回答问题。

### 多轮对话

实际场景不是单轮问答，而是连续对话：

```
用户: 给哪吒写首诗
助手: 红绫缠腕...
用户: 再改成七言的        ← 第二轮，依赖上文
助手: ...
```

SFT 要把整段对话拼成一个序列，所有用户部分 label=-100，所有助手部分计算 loss。

### Tokenizer（分词器）

模型不是按"字"处理的，而是按 token：

```
"哪吒降世" 可能被切成 → ["哪吒", "降", "世"]    （3个token）
"Hello"   可能被切成 → ["Hello"]                （1个token）
"unhappiness" → ["un", "happi", "ness"]         （3个token）
```

token 的切法直接影响模型能力。不同模型的 tokenizer 不一样，这也是为什么不同模型不能混用 tokenizer。

### 梯度和反向传播

我们说"训练就是调参数"，具体怎么调：

```
前向传播: 输入 → 模型计算 → 输出预测 → 和答案比 → 得到 loss
反向传播: loss → 对每个参数算梯度（该往大调还是小调、调多少）→ 更新参数
```

梯度就是"方向标"——告诉每个参数应该变大还是变小。5 亿个参数，每个都有自己的梯度，每一步训练都按梯度方向调整一点。

### 学习率

参数每次调多大幅度：

```
学习率大（0.01）:    每步跨大步 → 学得快但容易跳过最优解
学习率小（0.00001）: 每步跨小步 → 学得慢但更精细
```

SFT 通常用很小的学习率（1e-5 ~ 5e-5），因为不想把预训练学到的能力改太多。

### 灾难性遗忘

SFT 的副作用——学新的忘旧的：

```
微调前: 模型会写诗、会算数、会写代码
微调后: 诗写得更好了，但算数能力变差了
```

因为调参数的时候，和算数相关的参数也被改了。这也是推荐用 LoRA 的原因之一——冻住原模型参数，就不会遗忘。

### RLHF / DPO（SFT 之后的下一步）

大模型完整训练链路：

```
预训练 → SFT → RLHF/DPO
学语言    学听话   学什么回答更好
```

SFT 教模型"按格式回答"，但不区分好坏。比如：

```
用户: 如何学编程？
回答A: "学Python，从基础语法开始，推荐《Python编程从入门到实践》"  ← 有用
回答B: "编程很重要，你应该好好学习，加油！"                       ← 正确但没用
```

两个回答格式都对，SFT 分不出好坏。这就需要 RLHF 或 DPO。

**RLHF**（基于人类反馈的强化学习）：

```
第1步: 让模型对同一个问题生成多个回答
第2步: 人类标注员排序（A 比 B 好）
第3步: 用排序数据训练一个"打分模型"（Reward Model）
第4步: 用强化学习让模型去生成得分更高的回答
```

**DPO**（直接偏好优化）：RLHF 的简化版，不需要训练打分模型：

```
RLHF: 标注数据 → 训练打分模型 → 强化学习训练   （三步，复杂）
DPO:  标注数据 → 直接训练模型                   （一步，简单）
```

DPO 直接用"好回答 vs 坏回答"的配对数据训练，数学上等价但实现简单得多，现在越来越主流。

### 评估指标

怎么判断 SFT 效果好不好：

**Loss / 困惑度（Perplexity）**：模型对训练数据的拟合程度，越低越好。

**BLEU**：衡量生成文本和参考答案有多少词重叠（精确率角度）：

```
参考: 红绫缠腕，风火踏浪
预测: 红绫缠腕，风火踏浪     → BLEU = 1.0（完全一样）
预测: 红绫飞舞，烈火踏浪     → BLEU ≈ 0.5（一半词重叠）
```

**ROUGE**：和 BLEU 类似，但从召回率角度看——参考答案里有多少内容被预测覆盖到了：

```
参考: 红绫缠腕风火踏浪不屈不悔笑傲苍茫（16个字）
预测: 红绫缠腕（4个字）

BLEU  高（预测的4个字全在参考里 → 精确率高）
ROUGE 低（参考的16个字只覆盖了4个 → 召回率低）
```

BLEU 常用于翻译评估，ROUGE 常用于摘要评估（不能漏掉重要内容）。

**人工评估**：让人打分，最可靠但最贵。大模型领域越来越多使用"以大模型评大模型"（如 GPT-4 当评委）来降低成本。

## 八、关键概念速查

| 概念 | 一句话解释 |
|------|-----------|
| 预训练 | 读几万亿字的书，获得语言能力 |
| SFT | 用标注数据微调，教模型听指令 |
| Embedding | 查字典，把文字变成数字向量 |
| Transformer | 让字和字互相看，理解上下文 |
| Decoder | 只能看前文不能看后文的 Transformer，用于生成 |
| Encoder | 能看全文的 Transformer，用于理解 |
| Loss | 模型预测和正确答案的差距 |
| Epoch | 所有训练数据过一遍 |
| Label = -100 | 告诉模型这个位置不用学（SFT 的核心操作） |
| 参数 | 模型里的数字，训练就是调这些数字 |
| 过拟合 | 训练数据背太死，遇到新数据就不会了 |
| 泛化 | 模型能处理没见过的输入，举一反三 |
| LoRA | 冻住原模型，只加少量新参数微调，主流方案 |
| 数据增强 | 对现有数据做变换扩充，低成本提升泛化能力 |
| Early Stop | loss 够小就停止训练，防止过拟合 |
| Chat Template | 用特殊 token 区分角色的对话格式 |
| Tokenizer | 分词器，把文字切成 token，不同模型切法不同 |
| 梯度 | 告诉每个参数该调大还是调小的"方向标" |
| 反向传播 | 从 loss 反推每个参数的梯度，然后更新参数 |
| 学习率 | 参数每次调整的幅度，SFT 通常用很小的值 |
| 灾难性遗忘 | 学新任务时把旧能力忘了，LoRA 可缓解 |
| RLHF | 用人类偏好排序训练打分模型，再用强化学习优化 |
| DPO | RLHF 的简化版，直接用好/坏回答配对训练 |
| BLEU | 生成文本和参考答案的词重叠率（精确率角度） |
| ROUGE | 参考答案被生成文本覆盖的比例（召回率角度） |

## 九、运行方式

```bash
# 安装依赖
pip install torch transformers accelerate

# 训练（输出保存到 ./sft_output）
python train.py

# 推理验证
python infer.py
```

基座模型 Qwen/Qwen2.5-0.5B 约 1GB，首次运行自动从 HuggingFace 下载。
